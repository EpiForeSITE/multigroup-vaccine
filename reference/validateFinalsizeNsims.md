# Validate the Number of Simulations for `finalsize()`

Internal helper that checks `nsims` before stochastic simulations are
dispatched.

## Usage

``` r
validateFinalsizeNsims(nsims)
```

## Arguments

- nsims:

  Number of simulations requested by the caller.

## Value

A scalar integer simulation count.
