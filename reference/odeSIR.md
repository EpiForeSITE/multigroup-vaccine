# ODE system for multi-group SIR model TODO: Add documentation and @keywords internal flag unless we want it to be exported time parameter doesn't seem to be used, can this be removed safely?

ODE system for multi-group SIR model TODO: Add documentation and
@keywords internal flag unless we want it to be exported time parameter
doesn't seem to be used, can this be removed safely?

## Usage

``` r
odeSIR(time, state, par)
```
